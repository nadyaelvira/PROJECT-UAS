# Update Redirect Role Select

## Perubahan
Update redirect di role-select page agar sesuai alur yang logis:

- **Saya Anak** → `/child/elderly-data` (daftarkan data lansia dulu sebelum monitor)
- **Saya Lansia** → `/elderly/home` (langsung ke dashboard)

## File Diubah
- `app/(auth)/role-select/page.tsx` — ganti `/child` → `/child/elderly-data`

## Verification
1. `npm run dev`
2. Login → role select
3. Klik "Saya Anak" → masuk ke halaman data lansia
4. Klik "Saya Lansia" → masuk ke dashboard lansia
